"use client"

import { MuiStyles } from "@/types/mui"

export const styles: MuiStyles = {
  title: {
    fontWeight: 500,
    lineHeight: 1.71429,
    color: (theme) => theme.palette.text.primary
  }
}