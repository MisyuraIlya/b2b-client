export const numberFormat = new Intl.NumberFormat('en-GB', { maximumSignificantDigits: 6, useGrouping: false });
export const priceFormat = new Intl.NumberFormat('en-GB', { maximumSignificantDigits: 3, useGrouping: false });
